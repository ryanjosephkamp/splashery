{
  "name": "splashery",
  "version": "1.0.0",
  "private": true,
  "description": "Splashery: paint a planet. A pure-browser 3D paint toy.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1",
    "test": "playwright test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "prettier": "3.8.1"
  }
}
